{
  "schema_version": "autonomous-research-cases/v1",
  "eval": "autonomous-research-convergence",
  "cases": [
    {
      "id": "gate-contract",
      "task": "Research how comments gate derives decisions across CLI and MCP, then plan any parity fixes a human should approve before implementation.",
      "source_allowlist": ["pkg/comment/gate.go", "cmd/comments/gate.go", "pkg/mcp/gate.go", "pkg/comment/template.go", "pkg/comment/citations.go"],
      "golden_questions": [
        {"id": "G1", "question": "Which thread and suggestion states affect normal versus strict decisions?", "expected": "Normal mode fails only unresolved blocking threads; strict also fails unresolved non-blocking threads and pending suggestions.", "evidence": ["pkg/comment/gate.go"]},
        {"id": "G2", "question": "Where do template and citation violations enter each surface?", "expected": "Path-aware validation is shared by CLI validate, CLI gate, MCP validate, and MCP gate.", "evidence": ["pkg/comment/template.go", "cmd/comments/gate.go", "pkg/mcp/gate.go"]},
        {"id": "G3", "question": "What happens when a commented document has no template?", "expected": "Thread state is evaluated and structure is explicitly reported unchecked.", "evidence": ["cmd/comments/gate.go", "pkg/mcp/gate.go"]},
        {"id": "G4", "question": "Do review records authorize the gate?", "expected": "No; the newest record is context only and current state derives the decision.", "evidence": ["pkg/comment/gate.go"]}
      ]
    },
    {
      "id": "anchor-survival",
      "task": "Research how comment anchors survive document edits, including ambiguity and terminal failure, then plan a safe change to the cascade.",
      "source_allowlist": ["pkg/comment/anchor.go", "pkg/comment/reanchor.go", "pkg/comment/anchortext.go", "pkg/comment/validation.go", "pkg/comment/storage.go"],
      "golden_questions": [
        {"id": "G1", "question": "What is the ordered automatic re-anchor cascade?", "expected": "Exact position, exact text, normalized text, section fallback, then orphan.", "evidence": ["pkg/comment/anchor.go", "pkg/comment/validation.go"]},
        {"id": "G2", "question": "How are ambiguous text anchors handled?", "expected": "Creation rejects ambiguous quoted anchors instead of guessing a line.", "evidence": ["pkg/comment/anchortext.go"]},
        {"id": "G3", "question": "Who supplies explicit moves after an agent edit?", "expected": "The editing agent supplies comment ID to line or section moves; the automatic cascade is only a safety net.", "evidence": ["pkg/comment/reanchor.go"]},
        {"id": "G4", "question": "How is failure preserved for later repair?", "expected": "The thread becomes orphaned with its original line, reason, and timestamp retained in the sidecar.", "evidence": ["pkg/comment/validation.go", "pkg/comment/storage.go"]}
      ]
    },
    {
      "id": "template-contract",
      "task": "Research the complete template contract agents must satisfy before plan review, then plan a compatible extension.",
      "source_allowlist": ["pkg/comment/template.go", "pkg/comment/coverage.go", "pkg/comment/citations.go", "pkg/comment/style.go", "pkg/comment/actor.go", "cmd/comments/template.go", "pkg/mcp/template.go"],
      "golden_questions": [
        {"id": "G1", "question": "Which structural and prose-shape rules are deterministic?", "expected": "Required/order/word/subsection rules, question coverage, style, markers, and optional citation resolution are deterministic.", "evidence": ["pkg/comment/template.go", "pkg/comment/coverage.go", "pkg/comment/style.go", "pkg/comment/citations.go"]},
        {"id": "G2", "question": "How are omitted research clauses detected?", "expected": "Research questions are numbered and findings claim them with Q tags; unknown and uncovered IDs are violations.", "evidence": ["pkg/comment/coverage.go"]},
        {"id": "G3", "question": "What does seed persist besides threads?", "expected": "It records the template name so later gate calls enforce the same contract.", "evidence": ["cmd/comments/template.go", "pkg/mcp/template.go"]},
        {"id": "G4", "question": "Which decisions remain human-owned?", "expected": "Agents cannot resolve threads anchored in template sections marked zone human.", "evidence": ["pkg/comment/actor.go"]}
      ]
    },
    {
      "id": "review-baseline",
      "task": "Research how changed-since-last-review state is recorded and reported for one reviewer, then plan a safe extension without changing gate authority.",
      "source_allowlist": ["pkg/comment/baseline.go", "cmd/comments/gate.go", "cmd/comments/parity.go", "pkg/webreview/server.go"],
      "golden_questions": [
        {"id": "G1", "question": "Which review decisions replace the reviewer baseline?", "expected": "Approved and changes_requested verdicts replace it; a commented reply-pass leaves the prior verdict baseline intact.", "evidence": ["pkg/comment/baseline.go", "cmd/comments/gate.go", "pkg/webreview/server.go"]},
        {"id": "G2", "question": "Where is reviewer-specific baseline content stored?", "expected": "One sanitized-author Markdown file per document and reviewer lives under the document-local .comments/baselines directory.", "evidence": ["pkg/comment/baseline.go"]},
        {"id": "G3", "question": "How are edits and pure deletions represented?", "expected": "An LCS diff marks new or edited current lines and separately anchors pure deletion runs to a surviving current line; very large documents skip the quadratic diff.", "evidence": ["pkg/comment/baseline.go"]},
        {"id": "G4", "question": "How does status expose changed document regions?", "expected": "When a baseline exists, status reports changed-line and deletion counts plus innermost changed section paths in document order; it omits those keys when no baseline exists.", "evidence": ["pkg/comment/baseline.go", "cmd/comments/parity.go"]}
      ]
    },
    {
      "id": "citation-resolution",
      "task": "Research how file-line and thread citations remain unambiguous and peekable from a citing document, then plan a compatible browser citation view.",
      "source_allowlist": ["pkg/comment/citations.go", "pkg/comment/storage.go", "pkg/markdown/refs.go", "cmd/comments/main.go", "pkg/tui/keys_refpeek.go"],
      "golden_questions": [
        {"id": "G1", "question": "How are repository file citations resolved and rejected?", "expected": "Path-qualified references resolve from the citing document, while bare basenames are indexed repository-wide and rejected when ambiguous; missing files, reversed ranges, and lines past EOF are violations.", "evidence": ["pkg/comment/citations.go", "pkg/markdown/refs.go"]},
        {"id": "G2", "question": "Which apparent citations are deliberately not validated as file evidence?", "expected": "References without a line, thread references, and references inside fenced code blocks are skipped by file-line validation.", "evidence": ["pkg/comment/citations.go", "pkg/markdown/refs.go"]},
        {"id": "G3", "question": "How do same-document and cross-document thread citations resolve?", "expected": "thread:c1abc requires the citing document; thread:path.md#c1abc resolves relative paths against the citing document's directory rather than the caller working directory.", "evidence": ["pkg/comment/storage.go", "cmd/comments/main.go"]},
        {"id": "G4", "question": "What conversation is returned when a citation names a reply?", "expected": "Thread reads return the containing root thread so a citation peek includes the whole discussion rather than an isolated reply.", "evidence": ["pkg/comment/storage.go"]}
      ]
    }
  ]
}
